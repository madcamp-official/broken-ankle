# 멀티플레이 동기화 설계

2인 협력. 한 명이 호스트를 겸한다.

## 소유권

| 대상 | 주인 | 이유 |
|---|---|---|
| 각 플레이어 캐릭터 | 그 캐릭터를 조종하는 클라이언트 | 자기 입력이 즉시 반영돼야 조작감이 산다 |
| 몬스터 | 호스트 | 두 클라이언트가 각자 AI를 돌리면 서로 다른 곳으로 간다 |
| 조명, 배전반, 문 | 호스트 | 세계의 상태는 하나여야 한다 |
| 포획 상태 | 호스트 | 잡는 쪽이 몬스터이므로 |

호스트가 권위를 갖되 플레이어 이동만 소유자 권위로 둔다. 느린 공포 게임이라
치팅 방지보다 조작 반응이 중요하다.

---

## 1. 플레이어

### 보낼 것

| 값 | 출처 | 주기 | 비고 |
|---|---|---|---|
| 위치 | `Rigidbody2D.position` | 15~20Hz + 보간 | |
| 이동 입력 | `PlayerController.MoveInput` | 위치와 같은 패킷 | 원격 애니메이션이 이걸로 돈다 |
| 이동 모드 | `PlayerController.Mode` | 바뀔 때만 | 웅크림/걷기/달리기 |
| 손전등 On/Off | `FlashlightToggle.IsOn` | 바뀔 때만 | |
| 손전등 각도 | `Flashlight.rotation.z` | 위치와 같은 패킷 | 동료의 빔이 보여야 한다 |
| 헤드셋 착용 | `HearingRingToggle.IsOn` | 바뀔 때만 | 동료 몸에 그려진다. 2절과 헷갈리지 말 것 |

### 보내지 않을 것

`FacingDirection`과 애니메이터 파라미터는 보내지 않는다. 원격 쪽에서 이동
입력으로 다시 계산하면 같은 값이 나오고, 대역폭도 절반이 된다.

### 원격 캐릭터에서 할 일

`PlayerRig.Apply(viewer: false, controlled: false)` 로 입력을 끊고, 받은 값을
`PlayerController` 대신 주입한다. `PlayerAnimator` 는 그대로 두면 된다.

---

## 2. 순수 로컬 (동기화 금지)

시점에 속하는 것들이다. 동기화하면 두 화면이 같아져서 게임이 성립하지 않는다.

- `VisibilityMask` — 내가 보는 어둠
- `NoiseRing` — 내 헤드셋이 들려주는 것
- `FlashlightOcclusion`, `FlashlightBodyWash` — 내 빔의 렌더링
- `ControlsOverlay`, 카메라

`Ashburn.MD` 의 설정상 **두 플레이어가 서로 다른 것을 듣고 보는 것이 핵심**이다.
이 목록은 그 설계를 지키기 위한 경계선이다.

### 링과 착용은 다른 것이다

`NoiseRing` 은 동기화하지 않지만, **헤드셋을 쓰고 있는지는 동기화한다**(1절).
링은 내 헤드셋이 나에게만 들려주는 것이고, 착용은 동료도 눈으로 볼 수 있는
세계의 사실이다. `HeadsetSprites` 가 그것을 몸에 그린다.

경계는 "내 시점에 속하는가"이지 "같은 스위치가 켜는가"가 아니다.

---

## 3. 소리

### 전달 방식

`NoiseBus.Emit` 을 그대로 두고, 네트워크 계층이 이벤트를 브로드캐스트한다.

```
발소리 발생 → (owner) 네트워크로 { position, range, sourceId } 전송
            → 각 클라이언트가 로컬 NoiseBus 로 재방출
```

### 종류는 받는 쪽이 정한다

`NoiseKind` 를 보내면 안 된다. 같은 발소리가 **보내는 사람에게는 Self, 받는
사람에게는 Ally** 이기 때문이다. 보내는 건 "누가 냈는가"(sourceId)뿐이고,
받은 클라이언트가 자기 기준으로 종류를 정한다.

### 조작 지점

헤드셋이 소리를 왜곡한다는 설정이 여기 붙는다. 수신 측에서 이벤트를 가공한다.

- 동료 소리를 몇 초 늦게 재방출
- 동료의 발소리를 다른 위치에서 재생
- 한쪽에게만 몬스터 소리 전달
- 거리가 멀어지면 종류를 Unknown 으로 흐리기 (이미 링이 노란색으로 처리 중)

---

## 4. 몬스터

호스트만 `MonsterAI` 를 돌린다. 클라이언트는 결과만 받는다.

| 값 | 주기 |
|---|---|
| 위치 | 15~20Hz + 보간 |
| 속도 | 위치와 같은 패킷 (원격 애니메이션용) |
| 상태 (`MonsterState`) | 바뀔 때만 |
| 들고 있는 포획자 | 바뀔 때만 |

`Suspicion` 은 보내지 않는다. 플레이어에게 보이지 않는 내부 값이다.
나중에 UI로 노출하기로 하면 그때 추가한다.

몬스터의 발소리도 3번 경로를 탄다. 호스트에서 발생시키고 전원에게 뿌린다.

---

## 5. 포획과 구출

전부 호스트 권위. 놓치면 게임이 깨지므로 **신뢰성 있는 RPC**로 보낸다.

```
몬스터가 접촉 → 호스트가 판정 → Seize(captiveId)  브로드캐스트
동료가 E 누름 → 호스트에 요청 → 호스트가 검증 → Release(captiveId) 브로드캐스트
둥지 도착    → 호스트 판정   → Lost(captiveId)   브로드캐스트
```

구출은 **요청 후 검증**이다. 클라이언트가 직접 `Release` 를 부르면 거리 검증을
건너뛸 수 있다.

---

## 6. 세계 상태

배전반, 조명, 문. 호스트 권위이고 **늦게 들어온 클라이언트에게도 현재 상태가
전달돼야 한다.**

| 값 | 형태 |
|---|---|
| 대형 조명 On/Off | 동기화 변수 |
| 배전반 사용 여부 | 동기화 변수 |
| 문 잠김 상태 | 동기화 변수 |

RPC가 아니라 **상태 변수**로 두는 이유는, RPC는 이미 지나간 이벤트라 나중에
접속한 쪽이 놓치기 때문이다. 조명이 켜져 있다는 사실은 이벤트가 아니라 상태다.

`SimpleInteractable` 의 `UnityEvent` 는 로컬 연출용으로 두고, 실제 상태 변경은
호스트를 거치도록 한 겹 감싼다.

---

## 7. 스폰

씬에 손으로 놓은 `Player` 와 `Ally` 를 지우고 스폰 지점만 남긴다. 캐릭터는
`Assets/Resources/` 의 `Nathan`(A)과 `Grant`(B) 둘이다.

```
접속 완료 → 각 클라이언트가 자기 캐릭터만 스폰
         → 생긴 캐릭터가 스스로 PlayerRig.Apply(viewer: 내 것인가, controlled: 내 것인가)
```

`PlayerRig` 가 이미 그 한 줄만 받으면 되도록 준비돼 있다.

**호스트가 둘 다 스폰하면 안 된다.** `PhotonNetwork.Instantiate` 는 호출한
클라이언트를 소유자로 만들기 때문에, 호스트가 두 캐릭터를 만들면 B 캐릭터도
호스트 소유가 되어 위 소유권 표가 깨진다. `TransferOwnership` 으로 넘길 수는
있으나 한 프레임 늦고 실패 경로가 하나 는다.

역할을 적용하는 것도 스폰을 호출한 쪽이 아니다. 받는 클라이언트에도 상대
캐릭터가 생기고, **동료로 보여야 하는 것은 바로 그 사본**인데 만든 쪽 코드는
거기에 닿지 못한다. 그래서 캐릭터가 자기 자신에게 적용한다.

누가 A이고 누가 B인지 정하는 방법과 배선 절차는 `PlayerRoles.MD` 에 있다.

---

## 8. 무전기

PUN2는 상태만 나른다. 음성은 **Photon Voice 2**가 따로 붙는다. 룸 접속은
`PunVoiceClient` 가 PUN 룸을 따라가고, 캐릭터 프리팹에 `PhotonVoiceView` 를
붙이면 내 것이면 `Recorder`, 남의 것이면 `Speaker` 가 자동으로 연결된다.
7절의 스폰 흐름에 컴포넌트가 하나 더 붙는 수준이다.

### 변조는 전부 수신 측에서 한다

`Ashburn.MD` 7절에서 거짓말을 하는 주체는 **말하는 사람이 아니라 듣는 사람의
헤드셋**이다. 그래서 네트워크로는 **가공하지 않은 진짜 음성만** 보내고, 왜곡은
각 클라이언트가 자기 스피커에서 만든다.

3절의 원칙과 같다. 보내는 쪽은 사실만 보내고, 받는 쪽이 자기 몫의 거짓을 짓는다.

이렇게 두면 두 플레이어에게 서로 다른 왜곡을 주는 데 추가 대역폭이 0이고,
음성 SDK를 갈아끼워도 왜곡 코드는 그대로 남는다.

| | 어디서 도는가 | 네트워크 |
|---|---|---|
| 마이크 → 송신 | 소유자 | Photon Voice |
| 무전 소음 | 소유자가 발생, 3절 경로로 전파 | 있음 |
| 대역 제한·잡음·지연·피치 | 듣는 클라이언트 | **없음** |
| 동료 음성 저장·재생 | 듣는 클라이언트 | **없음** |
| 언제 되돌려줄지 | 호스트 판정 → RPC | 있음 |

### 구성

| 스크립트 | 붙는 곳 | 하는 일 |
|---|---|---|
| `RadioTransmitter` | 내 캐릭터 | PTT 입력, 채널 열림을 이벤트로 알림, `NoiseBus.Emit` |
| `PhotonRadioBridge` | 내 캐릭터 | 그 이벤트를 `Recorder.TransmitEnabled` 에 연결 |
| `RadioDsp` | 동료의 `Speaker` | `OnAudioFilterRead` 에서 대역 제한, 잡음, 지연, 피치 |
| `HeadsetLink` | 동료의 `Speaker` | 두 플레이어 거리 → `RadioDsp.Interference` |
| `VoiceArchive` | 동료의 `Speaker` | 동료 실제 음성을 저장했다가 나중에 재생 |
| `RadioLoopback` | 개발용 | 마이크를 자기 헤드셋으로 되돌려 혼자 튜닝 |

Photon을 아는 파일은 `PhotonRadioBridge` 하나뿐이다. 나머지 다섯은 SDK를
전혀 모르고, `RadioLoopback` 으로 접속 없이 혼자 들어보며 튜닝할 수 있다.

`Ashburn.Runtime.asmdef` 는 `PhotonRealtime`, `PhotonVoice.API`, `PhotonVoice`,
`PhotonUnityNetworking` 을 참조한다. 동료 스피커에 붙는 컴포넌트를 코드에서
만들게 되면 `PhotonVoice.PUN` 을 더한다.

### 말하는 것은 위험해야 한다

`Ashburn.MD` 8절이 "후반부에는 발소리보다 대화를 우선해서 추적한다"고 못박는다.
그래서 무전 송신은 3절 경로를 그대로 타고 `NoiseBus` 에 올라간다. 사거리는
걷기(7)와 달리기(13) 사이인 11로 둔다. 말하는 것이 달리는 것만큼 위험하면
아무도 말하지 않고, 공짜면 무전기가 기믹이 아니라 편의 기능이 된다.

### 동료 목소리 위조

실시간 음성 변환은 하지 않는다. 대신 **동료의 진짜 음성을 저장했다가 나중에
돌려준다.** `Ashburn.MD` 5절이 "상쇄한 모든 소리를 지하 서버에 원본으로
저장했다"고 이미 말하고 있으므로 설정에 그대로 들어맞고, 진짜 목소리라
어색한 구석이 없고, 모델도 성우도 필요 없다.

"아직 하지 않은 말"도 30초 전에 한 말을 맥락 밖에서 되돌려주면 성립한다.

저장은 오디오 스레드, 재생 결정은 메인 스레드다. 샘플 버퍼는 락 없이 쓰고
읽는 쪽이 링이 한 바퀴 돌았는지 앞뒤로 검사해서 겹치면 버린다. 락은 문장
경계 목록에만 걸린다.

**언제 재생할지는 호스트가 정한다.** 두 헤드셋이 각자 아무 때나 거짓말을 하면
연출이 아니라 잡음이다. `VoiceArchive.TryReplay()` 는 공개해 두고 호출 시점만
호스트 RPC로 내린다.

### 아직 안 한 것

- 씬과 프리팹 배선. `PunVoiceClient`, `PhotonVoiceView`, `Recorder`, 동료
  `Speaker` 쪽 DSP 3종
- 동료 `Speaker` 의 AudioSource 는 **Spatial Blend 0 (2D)** 이어야 한다.
  무전은 헤드셋 안에서 나는 소리지 동료가 서 있는 자리에서 나는 소리가 아니다
- `RadioTransmitter` 를 `PlayerRig` 의 `inputComponents` 에 넣기. 빠뜨리면
  원격 캐릭터도 자기 마이크를 연다
- `VoiceArchive.TryReplay()` 호출 시점을 정하는 호스트 로직
- 사전 녹음 가짜 대사("나한테 오지 마"). 완전 로컬 재생이라 네트워크와 무관하다
- 특정 단어를 다른 단어로 바꿔 들려주기

---

## 9. App ID — 새로 받은 사람이 할 일

저장소가 public이라 App ID는 커밋하지 않는다.

```
1. 저장소를 clone 하고 Unity 로 연다
2. Assets/Resources/ 폴더를 만든다 (없으면)
3. Assets > Create > Ashburn > Photon App Ids
   이름은 반드시 PhotonAppIds
4. Realtime / Voice 두 칸에 팀에게 받은 App ID 를 붙여넣는다
```

안 하면 실행할 때 콘솔에 무엇을 해야 하는지 경고가 뜨고 접속이 안 된다.

### 왜 이렇게 하는가

App ID 를 `PhotonServerSettings` 에 그냥 두고 커밋하지 않는 방법이 더 간단해
보이지만, 그 에셋에는 **`RpcList` 가 같이 들어 있다.**

PUN 은 RPC 를 이름이 아니라 이 리스트의 **인덱스**로 보낸다
(`PhotonNetworkPart.cs:385`). 두 기기의 리스트 순서가 다르면 에러가 나는 게
아니라 **조용히 다른 메서드가 호출된다.** 5절의 포획과 구출이 전부 RPC 이므로
이 에셋은 반드시 공유되어야 한다.

그래서 에셋은 App ID 칸을 비운 채로 커밋하고, `PhotonAppIds` 가 씬 로드 전에
값을 집어넣는다. PUN 과 Voice 둘 다 이 에셋에서 읽으므로
(`PunVoiceClient.cs:210` 이 PUN 의 `AppSettings` 를 복사한다) 주입 지점은
하나뿐이다.

플레이 모드를 나갈 때 넣었던 값을 도로 지운다. 안 그러면 에디터가 그 에셋을
더티로 판단하는 순간 App ID 가 커밋되는 파일에 쓰인다.

나중에 접속 코드를 직접 쓰게 되면 `ConnectUsingSettings(AppSettings)` 로
넘기고 에셋을 아예 건드리지 않는 쪽이 더 깔끔하다.

### 저장소에서 뺀 것

| | 크기 | 이유 |
|---|---|---|
| 각 패키지의 `Demos/` | 66MB | 안 쓴다 |
| `PhotonVoiceLibs/VisionOS/` | 70MB | 안 쓴다 |

`PhotonVoiceLibs/iOS/` 85MB 는 남겨 두었다. 플러그인은 플랫폼별로 골라
들어가므로 Windows 빌드 용량에는 영향이 없다. 저장소 크기만의 문제다.

---

## 10. 실제로 붙여 보기

설계가 아니라 절차다. 9절까지 갖췄다고 가정한다.

### 동시에 누를 필요가 없다

`NetworkGame` 은 `JoinOrCreateRoom("ashburn")` 을 부른다. 방이 있으면 들어가고
없으면 만든다는 뜻이므로 두 사람이 시각을 맞출 이유가 없다.

```
먼저 누른 쪽 → 방이 없으니 만들고 혼자 기다린다 → 마스터 → 슬롯 0 → Nathan
나중 누른 쪽 → 그 방에 들어간다                → 슬롯 1 → Grant
```

먼저 누른 쪽이 몇 분을 기다려도 방은 남아 있다. **순서만 있고 시각은 없다.**
누가 어느 캐릭터를 할지 정해서 하려면 그 사람이 먼저 누르면 된다.

### 각자 할 일

1. 같은 커밋에 있을 것. `gameVersion` 이 다른 클라이언트끼리는 서로를 보지
   못하고, `RpcList` 순서가 다르면 9절이 말한 대로 조용히 다른 메서드가 불린다
2. `PhotonAppIds` 를 갖출 것(9절). 없으면 접속을 시도조차 하지 않는다
3. **`Assets/Scenes/Systems.unity` 를 열 것.** `NetworkGame`, `PlayerSpawner`,
   UI 가 전부 여기 있다. 다른 씬을 열고 Play 하면 접속도 스폰도 없다
4. Play

### 캐릭터가 하나면 혼자인 것이다

7절에 따라 각 기기는 자기 캐릭터만 만든다. 그래서 Hierarchy 에 캐릭터가 하나뿐인
것은 **스폰이 깨진 것이 아니라 상대가 아직 안 온 것이다.** 붙으면 `Nathan` 과
`Grant` 둘이 보인다. 버그로 오진하기 쉬운 자리다.

### 혼자 시험하려면

Multiplayer Play Mode 패키지가 없으므로 두 번째 클라이언트는 빌드로 만든다.

```
빌드를 먼저 실행 (슬롯 0) → 에디터에서 Play (슬롯 1)
```

`Run In Background` 가 켜져 있어야 한다(켜 두었다). 포커스를 잃은 쪽은 프레임을
돌리지 않아 곧 끊긴다. 창으로 띄우려면
`-screen-fullscreen 0 -screen-width 1280 -screen-height 720 -logFile log.txt`.

빌드는 커밋되는 설정 파일을 말없이 고친다 — `ProjectSettings.asset` 의
`preloadedAssets` 와 Standalone 정적 배칭, URP 에셋의 셰이더 스트리핑 플래그,
`UnityConnectSettings.asset`. 아무도 내린 결정이 아니므로 빌드 뒤에는
`git status` 를 보고 되돌린다.

### 안 될 때

| 증상 | 원인 |
|---|---|
| 콘솔에 `No PhotonAppIds asset found` | 9절을 하지 않았다 |
| 캐릭터가 계속 하나, 에러 없음 | 상대가 안 왔다. **상대 쪽 콘솔을 봐야 한다** |
| `Could not join the room 'ashburn'` | 이미 두 명이 있다. `MaxPlayers = 2` |
| 잠깐 되다가 끊김 | `Run In Background` 가 꺼졌다 |
| Hierarchy 가 비고 아무 일도 없음 | `Systems` 가 아닌 씬에서 Play 했다 |

방 이름이 `ashburn` 으로 고정이라 같은 App ID 를 쓰는 다른 조와 부딪힌다. 둘만
쓰려면 `NetworkGame` 의 `Room Name` 을 바꾸고 **두 사람 다 같은 값**으로 맞춘다.
커밋해서 공유하는 편이 안전하다.

### 리전을 고정한 이유

`FixedRegion` 이 비어 있으면 클라이언트마다 핑으로 가까운 리전을 고른다. 서로 다른
지역에 있는 두 사람은 **같은 이름의 방을 각자의 리전에 따로 만들고 영원히 만나지
못한다.** 에러가 나지 않으므로 상대가 안 오는 것과 구분되지 않는다.

`kr` 로 박아 두었다. `FixedRegion` 이 있으면 `IsBestRegion` 이 거짓이 되어 핑을
건너뛰고 곧장 그 리전으로 붙는다. `DevRegion` 은 `ConnectToBest` 경로에서만 읽히는
값이므로 이제 아무것도 보지 않는다. 팀이 전부 다른 지역이면 값을 다시 정한다.

### 접속 없이 빨리 보려면

`Systems` 의 `NetworkGame` 오브젝트를 비활성화하면 `PlayerSpawner` 의 오프라인
채우기가 살아나 한 화면에 캐릭터 둘이 생기고 WSAD / 방향키로 나눠 조작한다. 문,
열쇠, 소음을 접속 없이 확인할 때 쓴다. 단 조작키 표시는 화면 주인 것만 나오므로
2P 키(방향키, R Shift, Num . , Num 0 / R Ctrl)는 화면에 나오지 않는다.

---

## 미정

- 재접속 처리
- 호스트가 나갔을 때
