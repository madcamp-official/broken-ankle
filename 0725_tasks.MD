**A: 시스템 담당**

- 플레이어 이동
- 카메라 추적
- 상호작용 시스템
- 몬스터 AI
- 게임 진행 상태
- 나중에 Photon 네트워크

**B: 콘텐츠 담당**

- 테스트 방/레벨 구성
- 타일맵 배치
- 캐릭터/오브젝트 스프라이트 정리
- 사운드 수집/폴더링
- 조명 분위기 테스트
- UI 초안


```
너: A + 플레이어/AI/네트워크
팀원: B + 간단한 상호작용
```

이유는 B가 게임의 모양을 빠르게 확인하기 좋고, A는 코드 구조를 안정적으로 잡아야 해서 한 명이 집중하는 게 좋아요.

첫 개발 단계는 이렇게 가면 됩니다.

**1단계: 둘이 같은 기준 맞추기**

첫 회의에서 딱 이것만 합의하세요.

```
브랜치 전략: 각자 브랜치에서 작업 후 PR
기준 씬: TestRoom
PPU: 32
타일: 32×32
해상도: 640×360
오늘 목표: 3분짜리 무서운 테스트 방
```

그리고 약속 하나:

```
같은 씬 파일을 동시에 수정하지 않는다.
```

**2단계: 1일짜리 목표 만들기**

처음 목표는 완성 게임이 아니라 이거예요.

```
플레이어가 어두운 방에서 움직이고,
배전반을 고치면 조명이 바뀌고,
소리를 내면 몬스터가 반응한다.
```

여기까지가 첫 수직 슬라이스의 씨앗입니다.

**3단계: 작업을 병렬로 나누기**

A가 할 일:

```
PlayerController 만들기
WASD 이동
벽 충돌
카메라 따라가기
Interaction 인터페이스/기반 만들기
```

B가 할 일:

```
TestRoom 씬 저장/관리
32×32 테스트 타일 배치
벽/바닥 Sorting Layer 정리
Player 스프라이트 프레임 확인
배전반 임시 오브젝트 배치
어두운 조명 테스트
```

둘이 충돌을 줄이려면 A는 주로 `Assets/Scripts`, B는 주로 `Assets/Scenes`, `Assets/Art`, `Assets/Prefabs`를 만지면 돼요.

**4단계: Prefab 중심으로 연결**

A가 플레이어 이동 스크립트를 만들면, B가 씬에 직접 이것저것 복붙하지 말고 `Player.prefab`으로 조립합니다.

추천 프리팹:

```
Player.prefab
Monster.prefab
BreakerBox.prefab
Door.prefab
InteractPrompt.prefab
```

씬은 최종 조립 공간, 프리팹은 재사용 가능한 부품이라고 보면 됩니다.

**5단계: 온라인은 바로 하지 말기**

Photon은 중요하지만, 첫날부터 온라인을 붙이면 디버깅이 확 어려워져요. 순서는 이게 좋아요.

```
로컬 1인 이동
로컬 상호작용
로컬 몬스터 반응
짧은 공포감 확인
그다음 2인 온라인 접속
```

온라인은 “게임이 성립한다”가 보인 뒤 붙이는 게 덜 힘들어요.

**6단계: 매일 끝날 때 공유 방식**

각자 작업 끝나면:

```
git pull
git add .
git commit -m "작업 내용"
git push
```

그리고 GitHub에서 PR을 만들어요. 둘이 바로 `main`에 막 push하지 말고:
브랜치를 나눠서 PR로 합치는 게 좋아요.

**이번 주 목표**

끝날 때 판단 기준은 이거 하나면 충분합니다.

```
온라인이 아니어도 좋으니,
한 명이 3분 동안 플레이했을 때
어둡고, 불안하고, 몬스터가 소리에 반응해서 긴장되는가?
```

이게 되면 계속 가면 되고, 안 되면 콘텐츠를 늘리지 말고 조명/사운드/몬스터 반응부터 다시 잡는 게 맞습니다.

내 추천 첫 작업은: **오늘은 `PlayerController`를 만들고, 팀원은 `TestRoom`에 작은 방 하나 만드는 것.**

그 둘이 만나면 게임이 처음으로 “보이기” 시작합니다.